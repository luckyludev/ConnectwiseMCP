{
  "name": "connectwise-mcp-v2",
  "version": "2.0.0-alpha.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "vitest run",
    "test:live-staging": "node scripts/staging-live-smoke.mjs",
    "scripts:check": "for file in scripts/*.mjs; do node --check \"$file\" || exit 1; done",
    "build:attachment-app": "vite build --config vite.attachment.config.ts && node scripts/embed-attachment-app.mjs",
    "pretypecheck": "npm run build:attachment-app",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.ui.json",
    "format:check": "prettier --check src tests ui scripts .github/workflows/v2-ci.yml docs/v2-foundation.md docs/legacy-read-surface-classification.md docs/v2-staging-acceptance-checklist.md docs/cloudflare-workers-staging-howto.md docs/architecture.md docs/chatgpt-connector.md docs/claude-setup.md docs/n8n-setup.md docs/copilot-studio-azure.md docs/azure-ad-checklist.md docs/entra-authentication.md docs/troubleshooting.md docs/README.md package.json tsconfig.json tsconfig.ui.json vite.attachment.config.ts wrangler.jsonc",
    "prebuild": "npm run build:attachment-app",
    "build": "wrangler deploy --env=\"\" --dry-run --outdir dist",
    "check": "npm run scripts:check && npm run typecheck && npm test && npm run format:check && npm audit --audit-level=high && npm run build"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "0.8.3",
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "0.20.1",
    "jose": "6.2.5",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260830.1",
    "@modelcontextprotocol/ext-apps": "1.7.5",
    "@modelcontextprotocol/sdk": "1.30.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "vite": "8.2.2",
    "vite-plugin-singlefile": "2.3.3",
    "vitest": "4.1.10",
    "wrangler": "4.127.1"
  }
}
